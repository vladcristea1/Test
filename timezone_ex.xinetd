# default: on
# description: The calculator server
service timezone_ex
{
	flags		= REUSE
	socket_type	= stream        
	wait		= no
	user		= nobody
	server		= /usr/local/bin/timezone_ex
	disable		= no
}

